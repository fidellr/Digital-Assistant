import { StyleSheet } from "react-native";

export default StyleSheet.create({
  progressBar: {
    flexDirection: "row",
    borderColor: "#b0d7fc",
    borderRadius: 4,
    borderWidth: 1,
    borderStyle: "solid",
  },
  innerProgressBar: {
    paddingTop: 6,
    backgroundColor: "#56a8fa",
    borderRadius: 4
  }
});
